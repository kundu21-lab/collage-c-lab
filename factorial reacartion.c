#include <stdio.h>
int sum(int);
int main()
{
	int num;
	printf ("enter a number"/num);
	scanf("%d",&num);
	return 0 ;}
	int fact(int num)
	{ if(n!=0)
		return (num!=num*f(num-1));
		else
		return num ;
	}
